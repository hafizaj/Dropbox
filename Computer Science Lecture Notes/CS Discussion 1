CS Discussion 
27 April 2017

// Copyright Ahmad Hafizuddin bin Ahmad Jaafar

stack s;
s.push(3);
s.push(-1);
int a = s.top();

only way to remove element from stack is by using s.pop();

Queue
represent horizontally
put things in the queue
but take the out : first in first out

queue.push(3);
q.push(-1);
int a= q.front();
q.pop();

Stacks are convenient to use for many problems
=====	=====	=====
Postfix evaluation
Prefix evaluation
Infix evalutaion

Postfix expression

"10 8 4 + 11 - * "
As we evaluate characrer, we want to do operation on what we store

When we find a number, we push it into the stack

s.push(10);
s.push(8);
s.push(4);
top one right operand, the bottom is the left operand
8 + 4
r = s.top(); s.pop();
l = s.top(); s.pop();

now this becomes 8 + 4
push the resultant into the stack
s.push(l+r);

next
11
12
10
===

12-11
s.push(1);

1
10
===

r = 1;
l = 10;
10
s.push(10);
at the end of the expression, then we get the result of 10.


cplusplus.com/reference/stack/stack/
cplusplus.com/reference/queue/queue/

cplusplus.com/doc/tutorials/arrays/
cplusplus.com/doc/tutorial/pointers/

Using libraries


Midterm Problem

City::~City()
{
	for(int i=0;i<m_nFlatulans;i__)
	{
		delete m_flatulans[i];
	}
}

int a = s.topp

std::stack::top
		reference& top();
const	reference& top() const;

when we change a reference, we make a chanfge to the eleents to he stack itself.






